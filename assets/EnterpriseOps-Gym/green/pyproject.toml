[project]
name = "ejepa-bench-enterpriseops-gym-green"
version = "0.1.0"
description = "Green agent runtime for the EnterpriseOps-Gym benchmark"
requires-python = ">=3.11"
dependencies = [
    "a2a-sdk>=0.3.5,<0.4.0",
    "httpx>=0.27,<1.0",
    "pydantic>=2.11,<3.0",
    "sse-starlette>=2.1,<3.0",
    "starlette>=0.47,<1.0",
    "uvicorn>=0.35,<1.0",
]

[project.optional-dependencies]
hf = [
    "datasets>=4.5,<5.0",
]

[tool.uv]
package = false
