# MCP server + mcp_react execution-core dependencies.
# The server itself only needs fastmcp + uvicorn to start and serve /mcp; the
# rest are pulled in when run_mcp_react_execution actually executes a task (the
# A2A-typed executor.py + the gym BenchmarkExecutor that connects to the tool
# MCP servers, and — for WM_STRATEGY=imagined — the world-model vLLM server).
fastmcp==2.6.1
uvicorn>=0.34,<1.0
httpx>=0.27,<1.0
pydantic>=2.11,<3.0
# executor.py imports the A2A types at module load:
a2a-sdk>=0.3.5,<0.4.0
# mcp_react execution core (gym BenchmarkExecutor + LangChain agent client).
# Pick the langchain provider extra(s) matching ENTERPRISEOPS_LLM_PROVIDER:
langchain>=0.1.0
langchain-core>=0.1.0
langchain-openai>=0.1.0
# langchain-anthropic>=0.1.0
# langchain-google-genai>=0.1.0
# langchain-deepseek>=0.1.0
mcp>=1.0.0
langchain-mcp-adapters>=0.1.0
datasets>=4.5.0
pandas>=2.0.0
numpy>=1.24.0
tqdm>=4.65.0
python-dotenv>=1.0.0
tabulate>=0.9.0
# Only needed for WM_STRATEGY=imagined with K_CONTROLLER=react_wm_rl_k
# (the vendored trained K-controller loads a torch/transformers checkpoint):
# torch
# transformers
